# Tasks

When running the system, many timeframes aren't fetched and I can see symbols with timframes that are null. Fix this so that all timeframes are fetched. Market Data Agent should retry until it succeeds or exceeds a limit